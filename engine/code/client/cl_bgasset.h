#ifndef CL_BGASSET_H
#define CL_BGASSET_H

/*
 * Legacy compatibility stub.
 *
 * The dynamic menu background subsystem was removed, but some incremental
 * builds can still reference this header via stale dependency files until
 * affected objects are rebuilt.
 */

#endif
